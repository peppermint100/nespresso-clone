import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles((theme) => ({
    container: {
        marginBottom: "40px",
    },
    header: {
        fontSize: "30px",
        fontWeight: 600,
        margin: "40px 20px",
    },
    itemContainer: {
        width: "100%",
    },
    item: {},
}));

export default useStyles